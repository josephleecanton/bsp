
/*  use this file to document PATHS & SYMBOLS that must be set across all modules
 *
 * adding them as below didn't seem to work as the define wasn't reachable from some parts
 * of the compile and build
 *
 * see PATHS & SYMBOLS in project properties


   #if !defined  (STM32F411xE)
   #define STM32F411xE
   #endif

*/
